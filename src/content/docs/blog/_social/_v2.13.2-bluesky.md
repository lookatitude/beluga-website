---
platform: bluesky
tag: v2.13.2
date: 2026-04-22
---

1/3
Beluga AI v2.13.2: `beluga eval <dataset>` runs your project once per row over exec-once IPC, scores with `ExactMatch` by default, and emits text, JSON, or JUnit XML. No bespoke runner code, no credentials needed for a first signal.

2/3
OTel wired in: `gen_ai.evaluation.*` spans, `beluga.eval.metric.score` histogram, `beluga.eval.run_id` for cross-signal joins. Scaffolds ship `.beluga/eval.yaml`, a 3-row smoke mock, and `make eval-ci`. Also in this window: v2.12.0 dev-loop (`run`, `dev`, `test`).

3/3
go get github.com/lookatitude/beluga-ai/v2@v2.13.2
Release notes: https://beluga-ai.org/blog/v2.13.2-release/
