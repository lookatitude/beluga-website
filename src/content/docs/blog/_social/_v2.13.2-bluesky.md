---
platform: bluesky
tag: v2.13.2
date: 2026-04-28
---

1/3
Beluga AI v2.12.0 + v2.13.0: `beluga dev` runs your agent against a mock LLM — no API calls, no cost. `beluga eval` adds CLI-driven evaluation with diffable per-metric scores. The full developer loop is now in the terminal.

2/3
`beluga test` uses the same mock LLM for CI. `beluga eval --scaffold` sets up the eval directory structure in one command. OTLP tracing is wired in from the start: same trace shape locally and in production.

3/3
DX-1 arc complete: init → scaffold → run/dev/test → eval. go get github.com/lookatitude/beluga-ai/v2@v2.13.2 — release notes: https://beluga-ai.org/blog/v2.13.2-release/
