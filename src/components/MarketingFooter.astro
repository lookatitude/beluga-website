---
import config from "@/config/config.json";
import menu from "@/config/menu.en.json";
import social from "@/config/social.json";
import ImageMod from "./ImageMod.astro";

const { site, params } = config;
const footerSections = menu.footer;
const socialLinks = social.main;
---

<footer class="marketing-footer">
  <!-- Gradient divider -->
  <div class="footer-divider"></div>

  <div class="footer-inner">
    <!-- Top section: Logo + tagline -->
    <div class="footer-top">
      <div class="footer-brand">
        <a href="/" class="footer-logo" aria-label={site.logo_text}>
          <ImageMod
            src={site.logo_darkmode}
            alt={site.logo_text}
            width={Number(site.logo_width)}
            height={Number(site.logo_height)}
            class="logo-dark"
            format="svg"
          />
          <ImageMod
            src={site.logo}
            alt={site.logo_text}
            width={Number(site.logo_width)}
            height={Number(site.logo_height)}
            class="logo-light"
            format="svg"
          />
        </a>
        <p class="footer-tagline">Build production-ready AI agents in Go.</p>
      </div>

      <!-- Link columns -->
      <div class="footer-columns">
        {
          Object.values(footerSections).map((section: any) => (
            <div class="footer-column">
              <h4 class="footer-column-title">{section.title}</h4>
              <ul class="footer-column-list">
                {section.links.map((link: any) => (
                  <li>
                    <a
                      href={link.url}
                      class="footer-link"
                      {...(link.url.startsWith("http") ? { target: "_blank", rel: "noopener noreferrer" } : {})}
                    >
                      {link.name}
                      {link.url.startsWith("http") && (
                        <svg class="external-icon" width="10" height="10" viewBox="0 0 12 12" fill="none">
                          <path d="M3.5 1H11v7.5M11 1L1 11" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
                        </svg>
                      )}
                    </a>
                  </li>
                ))}
              </ul>
            </div>
          ))
        }
      </div>
    </div>

    <!-- Bottom section -->
    <div class="footer-bottom">
      <div class="footer-copyright" set:html={params.copyright} />

      <div class="footer-bottom-right">
        <span class="footer-built-with">Built with Go</span>
        <div class="footer-social">
          {
            socialLinks.map((link: any) => (
              <a
                href={link.href}
                target="_blank"
                rel="noopener noreferrer"
                class="footer-social-link"
                aria-label={link.label}
              >
                {link.icon === "github" && (
                  <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z" />
                  </svg>
                )}
              </a>
            ))
          }
        </div>
      </div>
    </div>
  </div>
</footer>

<style>
  .marketing-footer {
    position: relative;
    margin-top: 5rem;
  }

  .footer-divider {
    height: 1px;
    background: var(--sl-color-hairline);
  }

  .footer-inner {
    max-width: 1740px;
    margin: 0 auto;
    padding: 2.5rem clamp(1.25rem, 4vw, 2.5rem) 1.5rem;
  }

  .footer-top {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
    margin-bottom: 2rem;
  }
  @media (min-width: 768px) {
    .footer-top {
      grid-template-columns: minmax(0, 1fr) minmax(0, 2fr);
      gap: 3rem;
    }
  }

  /* ── Brand ── */
  .footer-brand {
    display: flex;
    flex-direction: column;
    gap: 0.625rem;
  }
  .footer-logo {
    display: inline-flex;
    text-decoration: none;
  }
  .footer-logo img {
    height: 20px;
    width: auto;
  }
  .logo-light { display: none; }
  :root[data-theme="light"] .logo-dark { display: none; }
  :root[data-theme="light"] .logo-light { display: block; }

  .footer-tagline {
    font-size: 0.8125rem;
    line-height: 1.55;
    color: var(--sl-color-gray-3);
    max-width: 22rem;
    margin: 0;
  }

  /* ── Columns ── */
  .footer-columns {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1.5rem 2rem;
  }
  @media (min-width: 768px) {
    .footer-columns {
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 2rem;
    }
  }

  .footer-column-title {
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--sl-color-gray-3);
    margin: 0 0 0.75rem;
  }

  .footer-column-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.375rem;
  }

  .footer-link {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    font-family: var(--font-sans);
    font-size: 0.8125rem;
    line-height: 1.4;
    color: var(--sl-color-gray-2);
    text-decoration: none;
    transition: color 0.15s ease;
  }
  .footer-link:hover {
    color: var(--brand-300);
  }
  :root[data-theme="light"] .footer-link:hover {
    color: oklch(0.42 0.11 240);
  }

  .external-icon {
    opacity: 0.55;
    flex-shrink: 0;
  }

  /* ── Bottom ── */
  .footer-bottom {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    padding-top: 1.25rem;
    border-top: 1px solid var(--sl-color-hairline);
  }
  @media (min-width: 768px) {
    .footer-bottom {
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
    }
  }

  .footer-copyright {
    font-size: 0.75rem;
    color: var(--sl-color-gray-3);
  }
  .footer-copyright :global(a) {
    color: var(--brand-300);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 0.2s ease;
  }
  .footer-copyright :global(a:hover) {
    border-bottom-color: currentColor;
  }
  :root[data-theme="light"] .footer-copyright :global(a) {
    color: oklch(0.42 0.11 240);
  }

  .footer-bottom-right {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .footer-built-with {
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    letter-spacing: 0.04em;
    color: var(--sl-color-gray-3);
  }

  .footer-social {
    display: flex;
    align-items: center;
    gap: 0.25rem;
  }

  .footer-social-link {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0.375rem;
    color: var(--sl-color-gray-3);
    transition: color 0.15s ease;
    text-decoration: none;
    border-radius: 0.25rem;
  }
  .footer-social-link:hover {
    color: var(--sl-color-white);
  }
</style>
