---
import MarketingLayout from "@/layouts/MarketingLayout.astro";

const jsonLd = JSON.stringify({
  "@context": "https://schema.org",
  "@type": "WebPage",
  "name": "Beluga AI for Enterprise",
  "description": "Enterprise-grade AI agent infrastructure",
  "url": "https://beluga-ai.org/enterprise/"
});
---

<MarketingLayout
  title="Enterprise AI Agent Infrastructure"
  description="Enterprise-grade AI agent infrastructure with security, observability, guardrails, resilience, and durable execution for production teams."
  jsonLd={jsonLd}
>
  <!-- Block 15.1 — Hero -->
  <section class="ent-hero">
    <div class="ent-hero-inner">
      <h1 class="ent-hero-title">
        Enterprise-Grade <span class="ent-hero-title-accent">AI Agent Infrastructure</span>
      </h1>
      <p class="ent-hero-subtitle">
        Built for teams that ship to production — with the security, observability, and reliability primitives enterprises require.
      </p>
    </div>
  </section>

  <!-- Block 15.2 — Enterprise Capabilities Grid -->
  <section class="ent-section">
    <div class="ent-section-inner">
      <div class="ent-section-header">
        <h2 class="ent-section-title">Enterprise <span class="ent-title-muted">capabilities</span></h2>
      </div>

      <div class="ent-grid six-cards">
        <div class="ent-cap-card">
          <div class="ent-cap-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/>
              <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
              <circle cx="12" cy="16" r="1"/>
            </svg>
          </div>
          <h3 class="ent-cap-title">Security & Auth</h3>
          <p class="ent-cap-desc">RBAC/ABAC access control, capability-based sandboxing, default-deny network policies, and per-agent resource limits. Every action is auditable.</p>
        </div>

        <div class="ent-cap-card">
          <div class="ent-cap-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/>
              <circle cx="12" cy="12" r="3"/>
            </svg>
          </div>
          <h3 class="ent-cap-title">Observability</h3>
          <p class="ent-cap-desc">OTel GenAI conventions at every boundary. Token counting, cost tracking, structured logging, and health checks. Works with Datadog, Grafana, Jaeger, Splunk.</p>
        </div>

        <div class="ent-cap-card">
          <div class="ent-cap-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
            </svg>
          </div>
          <h3 class="ent-cap-title">Guardrails</h3>
          <p class="ent-cap-desc">Three-stage safety pipeline: input → output → tool guards. Prompt injection detection, PII filtering. NeMo and Guardrails AI adapters included.</p>
        </div>

        <div class="ent-cap-card">
          <div class="ent-cap-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/>
            </svg>
          </div>
          <h3 class="ent-cap-title">Resilience</h3>
          <p class="ent-cap-desc">Circuit breakers, hedge requests, adaptive retry, and provider-aware rate limiting. Your agents stay up when providers go down.</p>
        </div>

        <div class="ent-cap-card">
          <div class="ent-cap-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M12 2v4M12 18v4M4.93 4.93l2.83 2.83M16.24 16.24l2.83 2.83M2 12h4M18 12h4M4.93 19.07l2.83-2.83M16.24 7.76l2.83-2.83"/>
            </svg>
          </div>
          <h3 class="ent-cap-title">Durable Execution</h3>
          <p class="ent-cap-desc">Event-log-backed workflows surviving crashes, rate limits, and human-in-the-loop delays. Individual activities are independently retryable.</p>
        </div>

        <div class="ent-cap-card">
          <div class="ent-cap-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
              <circle cx="9" cy="7" r="4"/>
              <path d="M23 21v-2a4 4 0 0 0-3-3.87"/>
              <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
            </svg>
          </div>
          <h3 class="ent-cap-title">Multi-Tenancy</h3>
          <p class="ent-cap-desc">context.Context-based tenant isolation across all packages. No shared mutable state. Clean separation from request to response.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Block 15.3 — Deployment Story -->
  <section class="ent-section">
    <div class="ent-section-inner">
      <div class="ent-section-header">
        <h2 class="ent-section-title">Ship a single binary. <span class="ent-title-muted">Sleep well.</span></h2>
      </div>

      <div class="ent-deploy-grid">
        <div class="ent-deploy-item">
          <div class="ent-deploy-check">
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
              <path d="M4 10.5L8 14.5L16 6.5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div class="ent-deploy-text">
            <h3 class="ent-deploy-title">Single binary, no runtime dependencies</h3>
            <p class="ent-deploy-desc">No Python runtime, no pip dependencies, no virtual environments. One binary is your entire deployment artifact.</p>
          </div>
        </div>

        <div class="ent-deploy-item">
          <div class="ent-deploy-check">
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
              <path d="M4 10.5L8 14.5L16 6.5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div class="ent-deploy-text">
            <h3 class="ent-deploy-title">Tiny container images</h3>
            <p class="ent-deploy-desc">Container images from ~15MB using scratch base. Fast pulls, minimal attack surface.</p>
          </div>
        </div>

        <div class="ent-deploy-item">
          <div class="ent-deploy-check">
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
              <path d="M4 10.5L8 14.5L16 6.5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div class="ent-deploy-text">
            <h3 class="ent-deploy-title">Cross-compilation for any architecture</h3>
            <p class="ent-deploy-desc">Build for linux/amd64, linux/arm64, darwin, windows, or any Go-supported target from a single machine.</p>
          </div>
        </div>

        <div class="ent-deploy-item">
          <div class="ent-deploy-check">
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
              <path d="M4 10.5L8 14.5L16 6.5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div class="ent-deploy-text">
            <h3 class="ent-deploy-title">Kubernetes-native</h3>
            <p class="ent-deploy-desc">Health checks, graceful shutdown, and lifecycle management built in. Ready for production orchestration.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Block 15.4 — Compliance & Licensing -->
  <section class="ent-section">
    <div class="ent-section-inner">
      <div class="ent-section-header">
        <h2 class="ent-section-title">Open source. <span class="ent-title-muted">No surprises.</span></h2>
      </div>

      <div class="ent-compliance-grid">
        <div class="ent-compliance-card">
          <div class="ent-compliance-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
              <polyline points="14 2 14 8 20 8"/>
              <line x1="16" y1="13" x2="8" y2="13"/>
              <line x1="16" y1="17" x2="8" y2="17"/>
              <polyline points="10 9 9 9 8 9"/>
            </svg>
          </div>
          <h3 class="ent-compliance-title">MIT License</h3>
          <p class="ent-compliance-desc">No commercial restrictions, no copyleft. Use it however you need.</p>
        </div>

        <div class="ent-compliance-card">
          <div class="ent-compliance-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <line x1="18" y1="6" x2="6" y2="18"/>
              <line x1="6" y1="6" x2="18" y2="18"/>
            </svg>
          </div>
          <h3 class="ent-compliance-title">No telemetry</h3>
          <p class="ent-compliance-desc">No phone-home, no cloud lock-in. Your deployment is fully yours.</p>
        </div>

        <div class="ent-compliance-card">
          <div class="ent-compliance-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <rect x="2" y="2" width="20" height="8" rx="2" ry="2"/>
              <rect x="2" y="14" width="20" height="8" rx="2" ry="2"/>
              <line x1="6" y1="6" x2="6.01" y2="6"/>
              <line x1="6" y1="18" x2="6.01" y2="18"/>
            </svg>
          </div>
          <h3 class="ent-compliance-title">Fully self-hosted</h3>
          <p class="ent-compliance-desc">Data never leaves your infrastructure. Run on-prem or in your own cloud.</p>
        </div>

        <div class="ent-compliance-card">
          <div class="ent-compliance-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
              <polyline points="22 4 12 14.01 9 11.01"/>
            </svg>
          </div>
          <h3 class="ent-compliance-title">Compliance-ready</h3>
          <p class="ent-compliance-desc">Compatible with HIPAA, SOC 2, GDPR workflows (depends on provider selection).</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Block 15.5 — CTAs -->
  <section class="ent-cta">
    <div class="ent-section-inner">
      <h2 class="ent-cta-title">Ready to evaluate?</h2>
      <p class="ent-cta-desc">Whether you are exploring Beluga AI for your team or want to understand the architecture, we have you covered.</p>
      <div class="ent-cta-actions">
        <a href="https://github.com/lookatitude/beluga-ai/discussions" target="_blank" rel="noopener noreferrer" class="btn btn-primary">
          Evaluating for your team?
          <svg width="14" height="14" viewBox="0 0 12 12" fill="none">
            <path d="M3.5 1H11v7.5M11 1L1 11" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
          </svg>
        </a>
        <a href="/docs/architecture/" class="btn btn-outline-primary">
          Read the Architecture Overview
          <svg width="14" height="14" viewBox="0 0 12 12" fill="none">
            <path d="M1 6h10M7 2l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
          </svg>
        </a>
      </div>
    </div>
  </section>
</MarketingLayout>

<style>
  /* ── Hero ── */
  .ent-hero {
    padding: 8rem 1.5rem 4rem;
    text-align: center;
  }

  .ent-hero-inner {
    max-width: 860px;
    margin: 0 auto;
  }

  .ent-hero-title {
    font-size: clamp(2.5rem, 7vw, 4.5rem);
    font-weight: 800;
    line-height: 1.05;
    letter-spacing: -0.03em;
    margin: 0 0 1.5rem;
    color: var(--color-light, #f6f6f6);
  }

  :root[data-theme="light"] .ent-hero-title {
    color: var(--color-lightmode-light, #181818);
  }

  .ent-hero-title-accent {
    background: var(--color-primary-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .ent-hero-subtitle {
    font-size: clamp(1.0625rem, 2.2vw, 1.3125rem);
    color: var(--color-text, #999);
    max-width: 42rem;
    margin: 0 auto;
    line-height: 1.65;
  }

  :root[data-theme="light"] .ent-hero-subtitle {
    color: color-mix(in srgb, var(--color-lightmode-light, #181818) 60%, transparent);
  }

  /* ── Shared section styles ── */
  .ent-section {
    padding: 4rem 1.5rem;
  }

  .ent-section-inner {
    max-width: 1200px;
    margin: 0 auto;
  }

  .ent-section-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .ent-section-title {
    font-size: clamp(2rem, 4.5vw, 3.5rem);
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.03em;
    margin: 0;
    color: var(--color-light, #f6f6f6);
  }

  :root[data-theme="light"] .ent-section-title {
    color: var(--color-lightmode-light, #181818);
  }

  .ent-title-muted {
    color: color-mix(in srgb, var(--color-light, #f6f6f6) 40%, transparent);
  }

  :root[data-theme="light"] .ent-title-muted {
    color: color-mix(in srgb, var(--color-lightmode-light, #181818) 40%, transparent);
  }

  /* ── Block 15.2: Capabilities Grid ── */
  .ent-grid.six-cards {
    display: grid;
    gap: 1rem;
    grid-template-columns: 1fr;
  }

  @media (min-width: 640px) {
    .ent-grid.six-cards {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .ent-grid.six-cards {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .ent-cap-card {
    padding: 1.75rem;
    border-radius: 1rem;
    background: color-mix(in srgb, var(--color-dark, #151515) 80%, transparent);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid color-mix(in srgb, var(--color-light, #f6f6f6) 8%, transparent);
    position: relative;
    transition: transform 0.3s ease, border-color 0.3s ease;
  }

  .ent-cap-card::before {
    content: "";
    position: absolute;
    inset: 0;
    padding: 1px;
    background: var(--color-primary-gradient);
    border-radius: inherit;
    mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    mask-composite: exclude;
    -webkit-mask-composite: xor;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .ent-cap-card:hover::before {
    opacity: 0.6;
  }

  .ent-cap-card:hover {
    transform: scale(1.02);
  }

  :root[data-theme="light"] .ent-cap-card {
    background: color-mix(in srgb, var(--color-lightmode-dark, #f7f7f7) 80%, transparent);
    border-color: color-mix(in srgb, var(--color-lightmode-light, #181818) 8%, transparent);
  }

  .ent-cap-icon {
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 0.75rem;
    background: color-mix(in srgb, var(--color-primary) 12%, transparent);
    color: var(--color-primary, #5CA3CA);
    margin-bottom: 1rem;
  }

  .ent-cap-title {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--color-light, #f6f6f6);
    margin: 0 0 0.5rem;
    line-height: 1.3;
  }

  :root[data-theme="light"] .ent-cap-title {
    color: var(--color-lightmode-light, #181818);
  }

  .ent-cap-desc {
    font-size: 0.875rem;
    color: var(--color-text, #999);
    margin: 0;
    line-height: 1.65;
  }

  :root[data-theme="light"] .ent-cap-desc {
    color: color-mix(in srgb, var(--color-lightmode-light, #181818) 60%, transparent);
  }

  /* ── Block 15.3: Deployment Story ── */
  .ent-deploy-grid {
    max-width: 720px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .ent-deploy-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: 1.25rem 1.5rem;
    border-radius: 1rem;
    background: color-mix(in srgb, var(--color-dark, #151515) 80%, transparent);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid color-mix(in srgb, var(--color-light, #f6f6f6) 8%, transparent);
    transition: transform 0.2s ease, border-color 0.2s ease;
  }

  .ent-deploy-item:hover {
    transform: translateX(4px);
    border-color: color-mix(in srgb, var(--color-primary) 30%, transparent);
  }

  :root[data-theme="light"] .ent-deploy-item {
    background: color-mix(in srgb, var(--color-lightmode-dark, #f7f7f7) 80%, transparent);
    border-color: color-mix(in srgb, var(--color-lightmode-light, #181818) 8%, transparent);
  }

  .ent-deploy-check {
    width: 36px;
    height: 36px;
    min-width: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    background: color-mix(in srgb, var(--color-primary) 12%, transparent);
    color: var(--color-primary, #5CA3CA);
    margin-top: 2px;
  }

  .ent-deploy-text {
    flex: 1;
  }

  .ent-deploy-title {
    font-size: 1rem;
    font-weight: 600;
    color: var(--color-light, #f6f6f6);
    margin: 0 0 0.25rem;
    line-height: 1.4;
  }

  :root[data-theme="light"] .ent-deploy-title {
    color: var(--color-lightmode-light, #181818);
  }

  .ent-deploy-desc {
    font-size: 0.875rem;
    color: var(--color-text, #999);
    margin: 0;
    line-height: 1.6;
  }

  :root[data-theme="light"] .ent-deploy-desc {
    color: color-mix(in srgb, var(--color-lightmode-light, #181818) 60%, transparent);
  }

  /* ── Block 15.4: Compliance ── */
  .ent-compliance-grid {
    display: grid;
    gap: 1rem;
    grid-template-columns: 1fr;
  }

  @media (min-width: 640px) {
    .ent-compliance-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .ent-compliance-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  .ent-compliance-card {
    padding: 1.5rem;
    border-radius: 1rem;
    background: var(--color-dark, #151515);
    border: 1px solid color-mix(in srgb, var(--color-light, #f6f6f6) 8%, transparent);
    text-align: center;
    position: relative;
    transition: transform 0.3s ease;
  }

  .ent-compliance-card::before {
    content: "";
    position: absolute;
    inset: 0;
    padding: 1px;
    background: var(--color-primary-gradient);
    border-radius: inherit;
    mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    mask-composite: exclude;
    -webkit-mask-composite: xor;
    opacity: 0;
    transition: opacity 0.5s ease;
  }

  .ent-compliance-card:hover::before {
    opacity: 1;
  }

  .ent-compliance-card:hover {
    box-shadow: inset 0 0 30px color-mix(in srgb, var(--color-primary) 5%, transparent);
  }

  :root[data-theme="light"] .ent-compliance-card {
    background: var(--color-lightmode-dark, #f7f7f7);
    border-color: color-mix(in srgb, var(--color-lightmode-light, #181818) 8%, transparent);
  }

  .ent-compliance-icon {
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 0.75rem;
    background: color-mix(in srgb, var(--color-primary) 12%, transparent);
    color: var(--color-primary, #5CA3CA);
    margin: 0 auto 1rem;
  }

  .ent-compliance-title {
    font-size: 1.0625rem;
    font-weight: 600;
    color: var(--color-light, #f6f6f6);
    margin: 0 0 0.5rem;
    line-height: 1.3;
  }

  :root[data-theme="light"] .ent-compliance-title {
    color: var(--color-lightmode-light, #181818);
  }

  .ent-compliance-desc {
    font-size: 0.8125rem;
    color: var(--color-text, #999);
    margin: 0;
    line-height: 1.6;
  }

  :root[data-theme="light"] .ent-compliance-desc {
    color: color-mix(in srgb, var(--color-lightmode-light, #181818) 60%, transparent);
  }

  /* ── Block 15.5: CTA ── */
  .ent-cta {
    padding: 6rem 1.5rem;
    text-align: center;
  }

  .ent-cta-title {
    font-size: clamp(2rem, 4vw, 3.5rem);
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.02em;
    margin: 0 0 0.75rem;
    background: var(--color-primary-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .ent-cta-desc {
    font-size: 1.125rem;
    color: var(--color-text, #999);
    margin: 0 0 2rem;
    max-width: 36rem;
    margin-left: auto;
    margin-right: auto;
    line-height: 1.6;
  }

  :root[data-theme="light"] .ent-cta-desc {
    color: color-mix(in srgb, var(--color-lightmode-light, #181818) 60%, transparent);
  }

  .ent-cta-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    font-size: 0.9375rem;
    font-weight: 600;
    text-decoration: none;
    border-radius: 0.75rem;
    transition: transform 0.15s ease, filter 0.15s ease;
    cursor: pointer;
    border: none;
    font-family: inherit;
  }

  .btn:hover {
    transform: scale(1.03);
  }

  .btn-primary {
    background: var(--color-primary-gradient);
    color: #fff;
  }

  .btn-primary:hover {
    filter: brightness(1.1);
  }

  .btn-outline-primary {
    position: relative;
    background: transparent;
    color: var(--color-light, #f6f6f6);
  }

  .btn-outline-primary::before {
    content: "";
    position: absolute;
    inset: 0;
    padding: 2px;
    background: var(--color-primary-gradient);
    border-radius: inherit;
    mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    mask-composite: exclude;
    -webkit-mask-composite: xor;
  }

  .btn-outline-primary:hover {
    background: var(--color-primary-gradient);
    color: #fff;
  }

  :root[data-theme="light"] .btn-outline-primary {
    color: var(--color-lightmode-light, #181818);
  }
</style>
