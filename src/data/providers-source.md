<!-- GENERATED by internal/tools/provider-catalog — do not hand-edit -->
<!-- Intro and Related sections are hand-maintained; tables are generated. -->
<!-- Regenerate: go run ./internal/tools/provider-catalog > docs/reference/providers.md -->

# Reference: Providers

Every extensible package in Beluga follows the same registry model: a provider implements a small interface (1-4 methods), registers itself via `init()`, and becomes available at runtime through `Register` / `New` / `List`. Import a provider's package for its side-effect (`import _ "github.com/lookatitude/beluga-ai/llm/providers/openai"`) and call `llm.New("openai", cfg)` to get a fully wired instance with middleware, hooks, and tracing attached.

To add your own provider to any category below, follow the [Provider Template](../patterns/provider-template.md) pattern and the step-by-step [Custom Provider guide](../guides/custom-provider.md). The five-part recipe (implement the interface, write a factory, register in `init()`, write table-driven tests, document in godoc) applies identically to every category listed here.

The counts below reflect the actual directory tree. If a provider directory exists, it appears here. If it doesn't, it won't. To regenerate this file after adding a provider, run `go run ./internal/tools/provider-catalog > docs/reference/providers.md`.

## Observability — `o11y/providers`

| Provider | Description |
|---|---|
| `langfuse` | Package langfuse provides a Langfuse trace exporter for the Beluga AI observability system. |
| `langsmith` | Package langsmith provides a LangSmith trace exporter for the Beluga AI observability system. |
| `opik` | Package opik provides an Opik trace exporter for the Beluga AI observability system. |
| `phoenix` | Package phoenix provides an Arize Phoenix trace exporter for the Beluga AI observability system. |

**Count:** 4

## State stores — `state/providers`

| Provider | Description |
|---|---|
| `inmemory` | Package inmemory provides an in-memory implementation of the [state.Store] interface. |

**Count:** 1

## Workflow engines — `workflow/providers`

| Provider | Description |
|---|---|
| `dapr` | Package dapr provides a Dapr state store-backed [workflow.WorkflowStore] implementation for the Beluga AI workflow engine. |
| `inmemory` | Package inmemory provides an in-memory [workflow.WorkflowStore] for development and testing. |
| `inngest` | Package inngest provides an Inngest-backed [workflow.WorkflowStore] implementation for the Beluga AI workflow engine. |
| `kafka` | Package kafka provides a Kafka-backed [workflow.WorkflowStore] implementation for the Beluga AI workflow engine. |
| `nats` | Package nats provides a NATS JetStream KV-backed [workflow.WorkflowStore] implementation for durable workflow state persistence. |
| `temporal` | Package temporal provides a Temporal-backed [workflow.DurableExecutor] and [workflow.WorkflowStore] for the Beluga workflow engine. |

**Count:** 6

## LLM — `llm/providers`

| Provider | Description |
|---|---|
| `anthropic` | Package anthropic provides the Anthropic (Claude) LLM provider for the Beluga AI framework. |
| `azure` | Package azure provides the Azure OpenAI LLM provider for the Beluga AI framework. |
| `bedrock` | Package bedrock provides the AWS Bedrock LLM provider for the Beluga AI framework. |
| `bifrost` | Package bifrost provides a ChatModel backed by a Bifrost gateway for the Beluga AI framework. |
| `cerebras` | Package cerebras provides the Cerebras LLM provider for the Beluga AI framework. |
| `cohere` | Package cohere provides the Cohere LLM provider for the Beluga AI framework. |
| `deepseek` | Package deepseek provides the DeepSeek LLM provider for the Beluga AI framework. |
| `fireworks` | Package fireworks provides the Fireworks AI LLM provider for the Beluga AI framework. |
| `google` | Package google provides the Google Gemini LLM provider for the Beluga AI framework. |
| `groq` | Package groq provides the Groq LLM provider for the Beluga AI framework. |
| `huggingface` | Package huggingface provides the HuggingFace Inference API LLM provider for the Beluga AI framework. |
| `litellm` | Package litellm provides a ChatModel backed by a LiteLLM gateway for the Beluga AI framework. |
| `llama` | Package llama provides a Meta Llama model provider for the Beluga AI framework. |
| `mistral` | Package mistral provides the Mistral AI LLM provider for the Beluga AI framework. |
| `ollama` | Package ollama provides the Ollama LLM provider for the Beluga AI framework. |
| `openai` | Package openai provides the OpenAI LLM provider for the Beluga AI framework. |
| `openrouter` | Package openrouter provides the OpenRouter LLM provider for the Beluga AI framework. |
| `perplexity` | Package perplexity provides the Perplexity LLM provider for the Beluga AI framework. |
| `qwen` | Package qwen provides the Alibaba Qwen LLM provider for the Beluga AI framework. |
| `sambanova` | Package sambanova provides the SambaNova LLM provider for the Beluga AI framework. |
| `together` | Package together provides the Together AI LLM provider for the Beluga AI framework. |
| `xai` | Package xai provides the xAI Grok LLM provider for the Beluga AI framework. |

**Count:** 22

## Embedding — `rag/embedding/providers`

| Provider | Description |
|---|---|
| `cohere` | Package cohere provides a Cohere embeddings provider for the Beluga AI framework. |
| `google` | Package google provides a Google AI embeddings provider for the Beluga AI framework. |
| `inmemory` | Package inmemory provides a deterministic hash-based Embedder for testing. |
| `jina` | Package jina provides a Jina AI embeddings provider for the Beluga AI framework. |
| `mistral` | Package mistral provides an Embedder backed by the Mistral AI embeddings API. |
| `ollama` | Package ollama provides an Ollama embeddings provider for the Beluga AI framework. |
| `openai` | Package openai provides an OpenAI embeddings provider for the Beluga AI framework. |
| `sentence_transformers` | Package sentencetransformers provides an Embedder backed by the HuggingFace Inference API for Sentence Transformers models. |
| `voyage` | Package voyage provides a Voyage AI embeddings provider for the Beluga AI framework. |

**Count:** 9

## Vector stores — `rag/vectorstore/providers`

| Provider | Description |
|---|---|
| `chroma` | Package chroma provides a VectorStore backed by ChromaDB. |
| `elasticsearch` | Package elasticsearch provides a VectorStore backed by Elasticsearch's kNN search. |
| `inmemory` | Package inmemory provides an in-memory VectorStore for testing and small-scale use. |
| `milvus` | Package milvus provides a VectorStore backed by the Milvus vector database. |
| `mongodb` | Package mongodb provides a VectorStore backed by MongoDB Atlas Vector Search. |
| `pgvector` | Package pgvector provides a VectorStore backed by PostgreSQL with the pgvector extension. |
| `pinecone` | Package pinecone provides a VectorStore backed by the Pinecone vector database. |
| `qdrant` | Package qdrant provides a VectorStore backed by the Qdrant vector database. |
| `redis` | Package redis provides a VectorStore backed by Redis with the RediSearch module. |
| `sqlitevec` |  |
| `turbopuffer` | Package turbopuffer provides a VectorStore backed by the Turbopuffer serverless vector database. |
| `vespa` | Package vespa provides a VectorStore backed by the Vespa search engine. |
| `weaviate` | Package weaviate provides a VectorStore backed by the Weaviate vector database. |

**Count:** 13

## Document loaders — `rag/loader/providers`

| Provider | Description |
|---|---|
| `cloudstorage` | Package cloudstorage provides a DocumentLoader that loads files from cloud storage services (S3, GCS, Azure Blob). |
| `confluence` | Package confluence provides a DocumentLoader that loads pages from Atlassian Confluence via its REST API. |
| `docling` | Package docling provides a DocumentLoader that uses the IBM Docling API to convert documents (PDFs, DOCX, images, etc.) into structured content. |
| `firecrawl` | Package firecrawl provides a DocumentLoader that uses Firecrawl to crawl websites and extract their content as markdown. |
| `gdrive` | Package gdrive provides a DocumentLoader that loads files from Google Drive via the Google Drive REST API. |
| `github` | Package github provides a DocumentLoader that loads files from GitHub repositories via the GitHub API. |
| `notion` | Package notion provides a DocumentLoader that loads pages from Notion via its API. |
| `unstructured` | Package unstructured provides a DocumentLoader that uses the Unstructured.io API to extract structured content from files (PDFs, DOCX, images, etc.). |

**Count:** 8

## Memory stores — `memory/stores`

| Provider | Description |
|---|---|
| `dragonfly` | Package dragonfly provides a DragonflyDB-backed implementation of [memory.MessageStore]. |
| `inmemory` | Package inmemory provides in-memory implementations of the memory store interfaces. |
| `memgraph` | Package memgraph provides a Memgraph-backed [memory.GraphStore] implementation for the Beluga AI memory system. |
| `mongodb` | Package mongodb provides a MongoDB-backed implementation of [memory.MessageStore]. |
| `neo4j` | Package neo4j provides a Neo4j-backed [memory.GraphStore] implementation for the Beluga AI memory system. |
| `postgres` | Package postgres provides a PostgreSQL-backed implementation of [memory.MessageStore]. |
| `redis` | Package redis provides a Redis-backed implementation of [memory.MessageStore]. |
| `sqlite` | Package sqlite provides a SQLite-backed implementation of [memory.MessageStore]. |

**Count:** 8

## Guard — `guard/providers`

| Provider | Description |
|---|---|
| `azuresafety` | Package azuresafety provides an Azure Content Safety guard implementation for the Beluga AI safety pipeline. |
| `guardrailsai` | Package guardrailsai provides a Guardrails AI guard implementation for the Beluga AI safety pipeline. |
| `lakera` | Package lakera provides a Lakera Guard API guard implementation for the Beluga AI safety pipeline. |
| `llmguard` | Package llmguard provides an LLM Guard API guard implementation for the Beluga AI safety pipeline. |
| `nemo` | Package nemo provides an NVIDIA NeMo Guardrails guard implementation for the Beluga AI safety pipeline. |

**Count:** 5

## Prompt — `prompt/providers`

| Provider | Description |
|---|---|
| `file` | Package file provides a filesystem-based PromptManager that loads prompt templates from a directory of JSON files. |

**Count:** 1

## Eval — `eval/providers`

| Provider | Description |
|---|---|
| `braintrust` | Package braintrust provides a Braintrust evaluation metric for the Beluga AI eval framework. |
| `deepeval` | Package deepeval provides a DeepEval evaluation metric for the Beluga AI eval framework. |
| `ragas` | Package ragas provides RAGAS (Retrieval Augmented Generation Assessment) evaluation metrics for the Beluga AI eval framework. |

**Count:** 3

## Cache — `cache/providers`

| Provider | Description |
|---|---|
| `inmemory` | Package inmemory provides an in-memory LRU cache implementation for the Beluga AI framework. |

**Count:** 1

## STT (speech-to-text) — `voice/stt/providers`

| Provider | Description |
|---|---|
| `assemblyai` | Package assemblyai provides the AssemblyAI STT provider for the Beluga AI voice pipeline. |
| `deepgram` | Package deepgram provides the Deepgram STT provider for the Beluga AI voice pipeline. |
| `elevenlabs` | Package elevenlabs provides the ElevenLabs Scribe STT provider for the Beluga AI voice pipeline. |
| `gladia` | Package gladia provides the Gladia STT provider for the Beluga AI voice pipeline. |
| `groq` | Package groq provides the Groq STT provider for the Beluga AI voice pipeline. |
| `whisper` | Package whisper provides the OpenAI Whisper STT provider for the Beluga AI voice pipeline. |

**Count:** 6

## TTS (text-to-speech) — `voice/tts/providers`

| Provider | Description |
|---|---|
| `cartesia` | Package cartesia provides the Cartesia TTS provider for the Beluga AI voice pipeline. |
| `elevenlabs` | Package elevenlabs provides the ElevenLabs TTS provider for the Beluga AI voice pipeline. |
| `fish` | Package fish provides the Fish Audio TTS provider for the Beluga AI voice pipeline. |
| `groq` | Package groq provides the Groq TTS provider for the Beluga AI voice pipeline. |
| `lmnt` | Package lmnt provides the LMNT TTS provider for the Beluga AI voice pipeline. |
| `playht` | Package playht provides the PlayHT TTS provider for the Beluga AI voice pipeline. |
| `smallest` | Package smallest provides the Smallest.ai TTS provider for the Beluga AI voice pipeline. |

**Count:** 7

## S2S (speech-to-speech) — `voice/s2s/providers`

| Provider | Description |
|---|---|
| `gemini` | Package gemini provides the Gemini Live S2S provider for the Beluga AI voice pipeline. |
| `nova` | Package nova provides the Amazon Nova S2S provider for the Beluga AI voice pipeline. |
| `openai` | Package openai provides the OpenAI Realtime S2S provider for the Beluga AI voice pipeline. |

**Count:** 3

## Voice transport — `voice/transport/providers`

| Provider | Description |
|---|---|
| `daily` | Package daily provides the Daily.co transport provider for the Beluga AI voice pipeline. |
| `livekit` | Package livekit provides the LiveKit transport provider for the Beluga AI voice pipeline. |
| `pipecat` | Package pipecat provides the Pipecat transport provider for the Beluga AI voice pipeline. |

**Count:** 3

## VAD (voice activity detection) — `voice/vad/providers`

| Provider | Description |
|---|---|
| `silero` |  |
| `webrtc` | Package webrtc provides a pure Go WebRTC-style VAD (Voice Activity Detection) provider for the Beluga AI voice pipeline. |

**Count:** 2

## MCP — `protocol/mcp/providers`

| Provider | Description |
|---|---|
| `composio` | Package composio provides a Composio MCP integration for the Beluga AI protocol layer. |

**Count:** 1

## Server adapters — `server/adapters`

| Provider | Description |
|---|---|
| `chi` | Package chi provides a Chi-based ServerAdapter for the Beluga AI server package. |
| `connect` | Package connect provides a Connect-Go based ServerAdapter for the Beluga AI server package. |
| `echo` | Package echo provides an Echo-based ServerAdapter for the Beluga AI server package. |
| `fiber` | Package fiber provides a Fiber v3-based ServerAdapter for the Beluga AI server package. |
| `gin` | Package gin provides a Gin-based ServerAdapter for the Beluga AI server package. |
| `grpc` | Package grpc provides a gRPC-based ServerAdapter for the Beluga AI server package. |
| `huma` | Package huma provides a Huma-based ServerAdapter for the Beluga AI server package. |

**Count:** 7

---

**Total providers:** 110 across 19 categories.

## Related

- [Extensibility Patterns](../architecture/03-extensibility-patterns.md) -- the four mechanisms (interface, registry, hooks, middleware) every provider uses.
- [Registry + Factory pattern](../patterns/registry-factory.md) -- the registration model in detail.
- [Provider Template](../patterns/provider-template.md) -- building a new provider end-to-end.
- [Custom Provider guide](../guides/custom-provider.md) -- step-by-step walkthrough.
